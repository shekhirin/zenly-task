syntax = "proto3";
option go_package = "github.com/shekhirin/zenly-task/zenly/pb/enricher";

package enricher;

message Transport {
  enum Type {
    CAR = 0;
    TRAIN = 1;
    PLANE = 2;
  }
  Type type = 1;
}
