syntax = "proto3";
option go_package = "github.com/shekhirin/zenly-task/zenly/pb/enricher";

package enricher;

message PersonalPlace {
  enum Type {
    HOME = 0;
    WORK = 1;
    SCHOOL = 2;
  }
  Type type = 1;
}
